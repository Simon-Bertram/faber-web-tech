<!-- ArchitecturePreview.astro - Interactive Edge oRPC architecture mockup -->
<div
  class="rounded-lg border border-[var(--border-subtle)] bg-[var(--bg-surface)] p-6 shadow-xl transition-all hover:border-[var(--border-hover)]"
>
  <div
    class="flex items-center justify-between border-b border-[var(--border-subtle)] pb-4 mb-4"
  >
    <div class="flex items-center gap-2">
      <span class="h-3 w-3 rounded-full bg-red-500"></span>
      <span class="h-3 w-3 rounded-full bg-yellow-500"></span>
      <span class="h-3 w-3 rounded-full bg-green-500"></span>
      <span class="ml-2 font-mono text-xs text-[var(--text-muted)]"
        >edge-orpc.ts</span
      >
    </div>
    <span
      class="rounded bg-violet-500/10 px-2 py-0.5 font-mono text-xs font-semibold text-violet-500"
    >
      TYPESAFE RPC
    </span>
  </div>

  <div
    class="font-mono text-xs leading-relaxed text-[var(--text-secondary)] space-y-1 mb-4"
  >
    <p>
      <span class="text-[var(--secondary)]">const</span>
      <span class="text-[var(--primary)]">client</span>
      = createORPCClient({"{"}
    </p>
    <p class="pl-4">
      url: <span class="text-emerald-500">"https://api.faber.tech"</span>,
    </p>
    <p class="pl-4">
      runtime: <span class="text-violet-400">"Cloudflare Workers"</span>,
    </p>
    <p class="pl-4">
      database: <span class="text-violet-400">"D1 / Drizzle ORM"</span>,
    </p>
    <p>{"}"});</p>
    <p class="pt-2 text-[var(--text-muted)]">
      {"// Latency benchmark across 300+ edge PoPs:"}
    </p>
    <p class="text-emerald-500 font-semibold">
      Query latency: 28ms (Cold start: 0ms)
    </p>
  </div>

  <div
    class="flex items-center justify-between rounded bg-[var(--bg-surface-elevated)] p-2.5 font-mono text-xs border border-[var(--border-subtle)]"
  >
    <span class="text-[var(--text-muted)]">Edge Topology:</span>
    <span class="font-semibold text-[var(--text-primary)]"
      >Workers + D1 + KV</span
    >
  </div>
</div>

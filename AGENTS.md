# Agent instructions

This repo’s project map for agents is **[CONTEXT.md](CONTEXT.md)**. Read and follow it for every task (where to edit, boundaries, commands).

For framework how-tos, load the matching skill under `.agents/skills/` using the precedence in `.cursor/rules/skill-order.mdc`. Behavioral rules live in `.cursor/rules/`.
